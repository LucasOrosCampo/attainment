<Page x:Class="attainment.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Settings">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Application Settings"
                   FontFamily="{StaticResource ModernFont}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#525f7f"
                   Margin="25,30,25,20"/>

        <ScrollViewer Grid.Row="1"
                      Margin="25,5,25,5"
                      Style="{StaticResource ModernScrollViewer}"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="SettingsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Key}"
                                       VerticalAlignment="Center"
                                       FontFamily="{StaticResource ModernFont}"
                                       Foreground="#525f7f"
                                       Margin="0,0,12,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,25,25">
            <Button Content="Reload"
                    Margin="0,0,10,0"
                    Padding="30,10"
                    Style="{StaticResource GradientButton}"
                    Click="ReloadButton_Click"/>
            <Button Content="Save"
                    Padding="30,10"
                    Style="{StaticResource PrimaryActionButton}"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
